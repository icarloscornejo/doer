---
name: advise
description: >-
  Review specs, ACs, or code with configurable advisor personas (security,
  perf, mobile, a11y). Useful inside the doer pipeline (Stage 5) and standalone.
  Currently a planned skill; not yet operational.
version: 0.0.0
user-invocable: false
allowed-tools: []
---

# Advise. Configurable Persona Review

Planned satellite skill of the `wk` plugin. See `ROADMAP.md` for status and target ticket (`WK-8`).

Until implemented, the orchestrator uses a single generic reviewer in Stage 5.
