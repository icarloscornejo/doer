# Cost Protocol

Status: stub. Implementation tracked in ticket `WK-3` (see ROADMAP.md).

This file will document the per-run cost tracking, weekly TTL refresh of model rates, lazy fallback when a model is missing, and the never-blocking interactive override.
